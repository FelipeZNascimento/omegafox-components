import React from 'react';
import { Story } from '@storybook/react';
import { {{pascalCase name}} as Component } from '../components/';
import { I{{pascalCase name}}Props } from '../components/types';

export default {
  props: '',
  component: Component
};

const Template: Story<I{{pascalCase name}}Props> = (args) => <Component {...args} />;

export const {{pascalCase name}} = Template.bind({});

{{pascalCase name}}.args = {
  props: ''
};
